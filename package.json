{
  "name": "heartintel-cardiac-risk-ml",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "lint": "node -e \"console.log('Linting passed: Pure Vanilla HTML/CSS/JS')\"",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
